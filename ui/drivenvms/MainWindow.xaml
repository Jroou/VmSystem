<Window x:Class="drivenvms.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DrivenVMS — Системний Менеджер Віртуальних Машин" Height="500" Width="850"
        WindowStartupLocation="CenterScreen" Background="#F3F3F3" FontFamily="Segoe UI">

	<Window.Resources>
		<Style TargetType="Button">
			<Setter Property="Background" Value="#E1E1E1"/>
			<Setter Property="Foreground" Value="#1A1A1A"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Padding" Value="15,5"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Opacity" Value="0.9"/>
				</Trigger>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Background" Value="#CCCCCC"/>
					<Setter Property="Foreground" Value="#777777"/>
				</Trigger>
			</Style.Triggers>
		</Style>
	</Window.Resources>

	<Grid Margin="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<Border Grid.Row="0" Background="#FFFFFF" Padding="15,12" Margin="0,0,0,15" CornerRadius="6">
			<Border.Effect>
				<DropShadowEffect BlurRadius="10" Color="#000000" Opacity="0.05" ShadowDepth="1"/>
			</Border.Effect>
			<Grid>
				<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
					<Ellipse Width="10" Height="10" Fill="#0078D4" Margin="0,0,8,0"/>
					<TextBlock Text="Моніторинг Хост-Системи" FontWeight="Bold" FontSize="14" Foreground="#333333"/>
				</StackPanel>
				<TextBlock x:Name="SystemStatsText" FontSize="13" Foreground="#555555" HorizontalAlignment="Right" VerticalAlignment="Center" Text="Зчитування WinAPI метрик..."/>
			</Grid>
		</Border>

		<Border Grid.Row="1" Background="White" CornerRadius="6" Padding="5">
			<Border.Effect>
				<DropShadowEffect BlurRadius="10" Color="#000000" Opacity="0.05" ShadowDepth="1"/>
			</Border.Effect>
			<DataGrid x:Name="VmsDataGrid" AutoGenerateColumns="False"
                      IsReadOnly="True" SelectionMode="Single" HeadersVisibility="Column"
                      Background="Transparent" BorderThickness="0" RowHeight="35" FontSize="13" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE">
				<DataGrid.Resources>
					<Style TargetType="DataGridColumnHeader">
						<Setter Property="Background" Value="Transparent"/>
						<Setter Property="Foreground" Value="#666666"/>
						<Setter Property="FontWeight" Value="SemiBold"/>
						<Setter Property="Padding" Value="10,8"/>
						<Setter Property="BorderThickness" Value="0,0,0,1"/>
						<Setter Property="BorderBrush" Value="#DDDDDD"/>
					</Style>
					<Style TargetType="DataGridCell">
						<Setter Property="Padding" Value="10,0"/>
						<Setter Property="Template">
							<Setter.Value>
								<ControlTemplate TargetType="DataGridCell">
									<Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
										<ContentPresenter VerticalAlignment="Center"/>
									</Border>
								</ControlTemplate>
							</Setter.Value>
						</Setter>
						<Style.Triggers>
							<Trigger Property="IsSelected" Value="True">
								<Setter Property="Background" Value="#E0EEF9"/>
								<Setter Property="Foreground" Value="#0078D4"/>
							</Trigger>
						</Style.Triggers>
					</Style>
				</DataGrid.Resources>
				<DataGrid.Columns>
					<DataGridTextColumn Header="Назва віртуальної машини" Binding="{Binding Name}" Width="*"/>
					<DataGridTextColumn Header="Поточний статус" Binding="{Binding State}" Width="150"/>
					<DataGridTextColumn Header="Системний UUID" Binding="{Binding Uuid}" Width="280"/>
				</DataGrid.Columns>
			</DataGrid>
		</Border>

		<Grid Grid.Row="2" Margin="0,15,0,0">
			<Button x:Name="RefreshBtn" Content="Оновити дані" HorizontalAlignment="Left" Width="130" Height="38" Click="RefreshBtn_Click"/>

			<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
				<Button x:Name="StartBtn" Content="Запустити ВМ" Width="120" Height="38" Margin="0,0,10,0" Background="#107C41" Foreground="White" Click="StartBtn_Click"/>
				<Button x:Name="StopBtn" Content="Зупинити ВМ" Width="120" Height="38" Margin="0,0,10,0" Background="#A80000" Foreground="White" Click="StopBtn_Click"/>
				<Button x:Name="CreateBtn" Content="Створити нову" Width="140" Height="38" Background="#0078D4" Foreground="White" Click="CreateBtn_Click"/>
			</StackPanel>
		</Grid>
	</Grid>
</Window>